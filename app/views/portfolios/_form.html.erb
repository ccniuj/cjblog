<%= form_for @portfolio do |f| %>
  
  <% if @portfolio.errors.any? %>
    <div id='error_explanation'>
      <h2>
        <%= pluralize(@portfolio.errors.count, 'error') %> prohibited this article from being saved:
      </h2>
      <ul>
        <% @portfolio.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %></br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :description %></br>
    <%= f.cktext_area :description %>
  </p>
  <p>
    <%= f.label :url %></br>
    <%= f.cktext_area :url %>
  </p>
  <p>
    <%= f.label :cover %></br>
    <%= f.file_field :cover %>
  </p>
  <p>
    <%= f.submit %></br>
  </p>
<% end %>
